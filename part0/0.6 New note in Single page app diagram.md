```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: Default submission is prevented and notes list redrawn with new note appended by redrawNotes()

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: {"message":"note created"}
    deactivate server

    Note right of browser: Payload sent to server in JSON form
```